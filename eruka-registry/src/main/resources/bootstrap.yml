spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://localhost:8081
      fail-fast: true
      password: 123456
      username: user
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false