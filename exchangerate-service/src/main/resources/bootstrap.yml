spring:
  application:
    name: exchange-rate-service
  cloud:
    config:
      uri: http://localhost:8081
      fail-fast: true
      password: 123456
      username: user

server:
  servlet:
    context-path: /exchange
  port: 8090